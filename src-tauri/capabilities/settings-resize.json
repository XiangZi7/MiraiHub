{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-resize",
  "description": "Resize the frameless settings and column-tags windows from their edges and corners",
  "windows": ["settings", "column-tags"],
  "permissions": ["core:window:allow-start-resize-dragging"]
}
